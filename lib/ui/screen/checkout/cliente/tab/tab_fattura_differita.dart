import 'package:flutter/material.dart';

class TabFatturaDifferita extends StatelessWidget {
  const TabFatturaDifferita({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Configurazione fattura differita",
        style: TextStyle(color: Colors.grey),
      ),
    );
  }
}
