import 'package:flutter/material.dart';

class TabFidelityAvanzata extends StatelessWidget {
  const TabFidelityAvanzata({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Nessuna fidelity associata",
        style: TextStyle(color: Colors.grey),
      ),
    );
  }
}
